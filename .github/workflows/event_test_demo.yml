name: eventWorkflow

on:
  push:
    branches: [main]
  pull_request:
    branches: main
jobs:
  even_demo:
    runs-on: ubuntu-latest
    steps:
      - name: trigger_workflow
        run: echo "trigger workflow"
      
