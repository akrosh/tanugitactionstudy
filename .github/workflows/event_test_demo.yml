name: eventWorkflow_schedule

on:
  schedule:
    - cron: "10 11 * * *"
jobs:
  even_demo:
    runs-on: ubuntu-latest
    steps:
      - name: trigger_workflow
        run: echo "trigger workflow"
      
