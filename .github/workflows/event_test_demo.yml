name: eventWorkflow_schedule1

on:
  schedule:
    - cron: "45 9 * * *"
jobs:
  even_demo:
    runs-on: ubuntu-latest
    steps:
      - name: trigger_workflow
        run: echo "trigger workflow"
      
