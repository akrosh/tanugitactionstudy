name: multi-os

on:
  workflow_dispatch: 
jobs:
  even_demo:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest,windows-latest,macos-latest]
    steps:
      - name: trigger_workflow_multios
        run: echo "Testing on ${{matrix.os}}"
